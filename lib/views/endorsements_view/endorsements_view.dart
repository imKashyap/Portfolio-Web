import 'package:flutter/material.dart';

class EndorsementsView extends StatelessWidget {
  const EndorsementsView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Endorsement View'),
    );
  }
}
