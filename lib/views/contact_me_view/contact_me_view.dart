import 'package:flutter/material.dart';

class ContactMeView extends StatelessWidget {
  const ContactMeView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Contact me View'),
    );
  }
}
