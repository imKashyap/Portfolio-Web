import 'package:flutter/material.dart';

class MyWorksView extends StatelessWidget {
  const MyWorksView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Works View'),
    );
  }
}
